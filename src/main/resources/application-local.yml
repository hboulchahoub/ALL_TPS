server:
  port: 9091

spring:
    datasource:
      url: jdbc:mysql://localhost:3306/basepfe?createDatabaseIfNotExist=true
      driverClassName: com.mysql.jdbc.Driver
      username: root
      password : root

    jpa:
      database: mysql
      show-sql: true
      hibernate:
        ddl-auto: create

logging:
  level:
    org.springframework.web : INFO
    ma.gov.licence.pfe : DEBUG